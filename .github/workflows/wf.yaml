name: my checks
on:
    pull_request:
        types: [opened, synchronize, reopened, auto_merge_enabled]
    merge_group:

jobs:
    test:
        runs-on: ubuntu-latest
        name: Test Stuff
        steps:
          - name: Test step
            run: |
                echo "hello world"
                exit 0
    deplox:
        runs-on: ubuntu-latest
        name: Test Stuff Deploy
        environment: deploy
        steps:
          - name: Test deploy step
            run: |
                echo "hello yall"
                exit 0
